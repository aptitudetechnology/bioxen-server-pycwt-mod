# Server Configuration
HOST=0.0.0.0
PORT=8000

# Job Management
MAX_CONCURRENT_JOBS=10
JOB_TIMEOUT_SECONDS=3600

# Backend Configuration
DEFAULT_BACKEND=sequential
# Comma-separated list of allowed backends
AVAILABLE_BACKENDS=sequential,joblib,dask,gpu
